"""SEAM interactive initializer package.

This package owns the typed domain contract for the no-argument interactive
setup workflow: terminal result taxonomy, categorized failure kinds, exit-code
mapping, and secret-free typed errors.  Later todos add the Bash entry, prompt
ports, JSONC primitives, install/config adapters, validation, and reporting.
"""
