# v1.1.1-dev 版本变更与实验记录

## 版本元信息

| 项目 | 内容 |
| --- | --- |
| 分支 | `v1.1.1-dev` |
| 文档创建前基线提交 | `89918ba` (`docs: sync readme and docs with main`) |
| 日期 | 2026-05-29 |
| 本地仓库 | `/home/zihang/opencode_test/SEAM-merge-main-fusion-port-20260527` |

本文记录 `v1.1.1-dev` 当前开发版本相对 `main` 的主要新增能力，以及已经完成的 3 个关键 E2E 实验。文中绝对路径仅作为实验取证记录，不作为框架逻辑或复现脚本中的硬编码建议。

## 实验总览

| 实验 | Workflow | Run ID | 结果 | 总耗时 |
| --- | --- | --- | --- | --- |
| Deepwave custom-op final gate/report fixer | `src/workflows/musa_muxi_migration_v2_container_baseaware_entryfix_reportfix_no_forced_routing.yaml` | `e2e-v3-20a33ec8a787` | PASS | `3221.402s` |
| GLM-OCR vLLM | `src/workflows/local_musa_muxi_vllm_glmocr_gpu5.yaml` | `e2e-v3-7866149fdc90` | PASS | `10690.126s` |
| GLM-OCR SGLang | `src/workflows/local_musa_muxi_sglang_glmocr_gpu6.yaml` | `e2e-v3-910ede6132ed` | PASS | `4721.223s` |

## 追加验证：PPU 复现实验（2026-05-29）

在完成 `v1.1.1-dev` 的 PPU GLM-OCR SGLang workflow 移植以及三项 PPU 容器生命周期 guardrail prompt 新增后，于本地 `v1.1.1-dev` 检出上复现了当前版本的 DeepWave、MinerU 和 GLM-OCR SGLang 三条 PPU 迁移链路。所有复现实验均在 PPU/NPU 容器环境中以长 E2E 方式执行，外层 timeout 均设置为 3 小时以上。

| 项目 | Workflow | Summary | 结果 | 耗时 | Entry |
| --- | --- | --- | --- | --- | --- |
| DeepWave PPU normal-entry | `src/workflows/ppu_migration_normal_entry_057_experiment.yaml` | `e2e-reports/src/20260528_230658/summary.json` | PASS | `2575.348s` | `/usr/local/bin/python3 /workspace/057_example_fwi.py` |
| MinerU PPU baseaware | `src/workflows/ppu_migration_v2_auto_vllm018_smoke_baseaware_keep.yaml` | `e2e-reports/src/20260528_235013/summary.json` | PASS | `2803.872s` | `/usr/local/bin/python3 /workspace/demo/demo.py` |
| GLM-OCR PPU SGLang | `src/workflows/ppu_migration_glmocr_sglang.yaml` | `e2e-reports/src/20260529_003717/summary.json` | PASS | `1832.426s` | `glmocr parse examples/source/table.png --mode selfhosted --output results/` |

### 本次验证前置改动

- 从当前分支移植了 `src/workflows/ppu_migration_glmocr_sglang.yaml`，实现 PPU/SGLang 容器内 GLM-OCR 迁移链路。
- 在以下文件中新增了容器生命周期 guardrail prompt：
  - `src/prompts/phase_error_recovery_container_ppu.md`
  - `src/prompts/repair_code_adapter_container_ppu.md`
  - `src/prompts/repair_dependency_fixer_container_ppu.md`
- 三条 workflow 在长 E2E 执行前均已通过 `load_workflow()` parser smoke check，workflow 结构解析无报错。

### 结论

经过上述 PPU workflow 与 prompt 前置准备，`v1.1.1-dev` 可以在 PPU/NPU 容器中复现当前版本的 DeepWave、MinerU 和 GLM-OCR SGLang 三条 PPU 实验，E2E 命令均使用大于 3 小时的外层 timeout。三条链路最终均为 PASS，Phase 5 repair loop 均在配置的迭代上限内收敛。

## 实验一：Deepwave custom-op final gate/report fixer

### 目标

验证当前框架在 custom-op 路径下可以通过 final-gate validator 的真实结果驱动修复，并能把 report-fixer 作为独立 repair role 使用，避免让通用 fixer 被报告格式修复任务污染。

### 配置

| 项目 | 内容 |
| --- | --- |
| 原始项目 | `/home/zihang/opencode_test/original_projects/deepwave` |
| Workflow | `src/workflows/musa_muxi_migration_v2_container_baseaware_entryfix_reportfix_no_forced_routing.yaml` |
| OpenCode server | `http://127.0.0.1:4097` |
| Server 配置来源 | `/home/zihang/opencode_test/SEAM` |
| 输出目录 | `e2e-reports/src/20260527_141349` |
| 输出项目 | `/home/zihang/opencode_test/output_projects/deepwave_20260527_141349` |
| Run ID | `e2e-v3-20a33ec8a787` |

### 执行命令

```bash
python3 src/tests/e2e/e2e_test_v3.py \
  --project-dir /home/zihang/opencode_test/original_projects/deepwave \
  --workflow-path src/workflows/musa_muxi_migration_v2_container_baseaware_entryfix_reportfix_no_forced_routing.yaml \
  --max-phase5-iter 10 \
  --server-url http://127.0.0.1:4097 \
  --server-no-auto-start \
  --verbose
```

### 结果

| 指标 | 结果 |
| --- | --- |
| E2E 状态 | PASS |
| Phase 5 | PASSED |
| 总耗时 | `3221.402s` |
| Commands | `16` |
| Sessions | `4` |

### 关键证据

- analyzer 成功将修复路由到 `final_gate_report_fixer`。
- report fixer 已执行，产生 `phase_fix_report_*` 相关 artifact。
- finalGate runner 输出 `passed=True`，`error_count=0`。
- 该实验验证了 report-fixer 与 final-gate validator-runner 的闭环：agent 不再模拟 validator 结果，而是通过可执行 runner 获得真实最终校验反馈。

### 主要修复点

- 在 workflow 中加入 `final_gate_report_fixer` repair role 和 `fix_report` / `imp_fix_report` 路由。
- 在 final-gate validator 输出中保留完整错误列表和 `error_count`，避免只暴露截断后的摘要。
- 让 report-fixer 使用独立 prompt，避免污染 `dependency_fixer`、`code_adapter`、`operator_fixer` 的任务目标。

## 实验二：GLM-OCR vLLM

### 目标

验证当前框架能在 MACA/vLLM 容器中完成 GLM-OCR 的本地 vLLM 推理烟测，复现历史手工实验中模型加载、多模态预处理、M-RoPE 和 generation 链路可以跑通的结论。

### 配置

| 项目 | 内容 |
| --- | --- |
| 原始项目 | `/home/zihang/opencode_test/original_projects/GLM-OCR` |
| Workflow | `src/workflows/local_musa_muxi_vllm_glmocr_gpu5.yaml` |
| User constraints | `src/local_constraints_glmocr_vllm.md` |
| 输出目录 | `e2e-reports/src/20260528_010341` |
| 输出项目 | `/home/zihang/opencode_test/output_projects/GLM-OCR_20260528_010341` |
| Run ID | `e2e-v3-7866149fdc90` |
| Entry script | `/opt/conda/bin/python3 /workspace/smoke_glmocr_vllm.py` |

### 执行命令

```bash
python3 src/tests/e2e/e2e_test_v3.py \
  --project-dir /home/zihang/opencode_test/original_projects/GLM-OCR \
  --workflow-path src/workflows/local_musa_muxi_vllm_glmocr_gpu5.yaml \
  --user-constraints src/local_constraints_glmocr_vllm.md \
  --max-phase5-iter 8 \
  --server-url http://127.0.0.1:4097 \
  --server-no-auto-start \
  --verbose
```

### 结果

| 指标 | 结果 |
| --- | --- |
| E2E 状态 | PASS |
| 总耗时 | `10690.126s` |
| Phase 5 耗时 | `10074.533s` |
| Phase 5 iterations | `6` |
| Commands | `19` |
| Sessions | `5` |

结果文件：

```text
/home/zihang/opencode_test/output_projects/GLM-OCR_20260528_010341/results/glmocr_vllm_smoke.json
```

结果内容摘要：

~~~json
{
  "status": "success",
  "response_text": "```markdown\n\n```",
  "elapsed_seconds": 62.73
}
~~~

### 关键修复链路

1. 修正 vLLM `--limit-mm-per-prompt` 参数格式，使用 JSON 字符串形式 `{"image": 1}`。
2. 处理 GLM-OCR checkpoint 中额外 MTP layer 权重，跳过 `layers.16` 相关非推理必需权重。
3. 处理 vLLM/transformers 对 `glm_ocr` 的兼容问题，使模型结构和权重加载可以继续执行。
4. 针对 MetaX vendor torch 缺少 memory-efficient attention 的情况，引入大序列 chunked attention workaround，避免 vision encoder profile 阶段 OOM。
5. 将 readiness 判定改为以 HTTP health/model endpoint 轮询为主，避免依赖 vendor fork 不一定输出的 uvicorn 日志。
6. 修复 GLM-OCR M-RoPE 调用中 `mm_token_type_ids` 参数不匹配问题，并处理空 `video_grid_thw` 场景。

### 结论

当前框架能够通过 repair loop 自动推进 GLM-OCR vLLM 路径，从参数格式、依赖兼容、权重加载、多模态 profile、服务 readiness 到实际推理输出，最终完成 E2E PASS。

## 实验三：GLM-OCR SGLang

### 目标

验证当前框架能在 MACA/SGLang 容器中完成 GLM-OCR selfhosted 模式解析，包含 SGLang 服务启动、布局模型依赖修复、本地模型加载、OCR pipeline 输出生成。

### 配置

| 项目 | 内容 |
| --- | --- |
| 原始项目 | `/home/zihang/opencode_test/original_projects/GLM-OCR` |
| Workflow | `src/workflows/local_musa_muxi_sglang_glmocr_gpu6.yaml` |
| User constraints | `/home/zihang/opencode_test/original_projects/GLM-OCR/ADAPTATION_REQUIREMENTS.md` |
| 输出目录 | `e2e-reports/src/20260528_040322` |
| 输出项目 | `/home/zihang/opencode_test/output_projects/GLM-OCR_20260528_040322` |
| Run ID | `e2e-v3-910ede6132ed` |
| Entry script | `/opt/conda/bin/python3 -m glmocr parse examples/source/table.png --mode selfhosted --output results/` |

### 执行命令

```bash
python3 src/tests/e2e/e2e_test_v3.py \
  --project-dir /home/zihang/opencode_test/original_projects/GLM-OCR \
  --workflow-path src/workflows/local_musa_muxi_sglang_glmocr_gpu6.yaml \
  --user-constraints /home/zihang/opencode_test/original_projects/GLM-OCR/ADAPTATION_REQUIREMENTS.md \
  --max-phase5-iter 8 \
  --server-url http://127.0.0.1:4097 \
  --server-no-auto-start \
  --verbose
```

### 结果

| 指标 | 结果 |
| --- | --- |
| E2E 状态 | PASS |
| 总耗时 | `4721.223s` |
| Phase 5 耗时 | `4217.005s` |
| Phase 5 iterations | `8` |
| Commands | `23` |
| Sessions | `5` |

输出文件：

```text
/home/zihang/opencode_test/output_projects/GLM-OCR_20260528_040322/results/table/table.md
/home/zihang/opencode_test/output_projects/GLM-OCR_20260528_040322/results/table/table.json
/home/zihang/opencode_test/output_projects/GLM-OCR_20260528_040322/results/table/table_model.json
/home/zihang/opencode_test/output_projects/GLM-OCR_20260528_040322/results/table/layout_vis/table.jpg
```

`table.md` 中已生成 HTML table 形式 OCR 结果，包含“项目”“本期金额”“上期金额”“说明”等表头和多行表格内容。

### 关键修复链路

1. 升级 transformers 以支持 `PPDocLayoutV3ForObjectDetection` 和 `PPDocLayoutV3ImageProcessor`。
2. 在网络受限环境中通过 ModelScope 获取 PPDocLayoutV3 模型，并将配置指向本地模型路径。
3. 修复 GLM-OCR model weights symlink/权重不可用问题，确保 SGLang 能读取本地模型。
4. 新增 `run_selfhosted.py` wrapper：启动 SGLang、轮询 `/v1/models` readiness、执行 parse、最后清理服务。
5. 避开被占用的默认 8080 端口，改用 wrapper 管理的 `51888` 端口并传递 `pipeline.ocr_api.api_port`。
6. 通过 `glmocr/__main__.py` 中对 `--mode selfhosted` 的分流，确保即使框架仍传入原始 parse 命令，也能走 selfhosted wrapper 路径。

### 结论

当前框架能够完成 GLM-OCR SGLang selfhosted 路径的自动修复和最终验证，最终生成可检查的 OCR 输出文件。

## 相对 main 的新增能力

`v1.1.1-dev` 相对 `main` 的主要新增能力包括：

- **report-fixer/final-gate validator-runner 支持**：custom-op final gate 失败时可以通过独立 report fixer 修复最终报告，而不是让通用 fixer 猜测 validator 要求。
- **动态 repair role 描述**：analyzer prompt 使用 `{repair_role_descriptions}`，根据当前 workflow 可用角色动态注入，避免硬编码三角色。
- **available-role-aware analyzer/routing**：框架根据 workflow 中实际存在的 repair phase/route 控制 analyzer 可选角色，减少无效路由。
- **final-gate validator command/contract injection**：report fixer 可以看到真实可执行的 final-gate validator 命令和 contract 信息。
- **完整 final-gate 错误输出**：final-gate runner 保留 `error_count` 和完整错误列表，避免只给截断 summary 导致 agent 无法修复。
- **report-fixer prompt templates**：新增 generic/container/MUSA report-fixer prompts，用于最终报告和 final-gate schema 修复。
- **GLM-OCR 本地实验 workflows**：新增 `local_musa_muxi_vllm_glmocr_gpu5.yaml`、`local_musa_muxi_sglang_glmocr_gpu6.yaml` 和 `src/local_constraints_glmocr_vllm.md`，用于复现 vLLM/SGLang 实验。
- **测试覆盖**：新增或更新 repair role prompts、repair loop、workflow executor 相关测试，覆盖动态角色、report-fixer 路由和 validator-runner 行为。

## 复现实验注意事项

- 长 E2E 必须设置足够外层 timeout；GLM-OCR vLLM 本次耗时接近 3 小时，建议外层至少 4 小时以上。
- 为保证可比性，应使用与实验一致的 OpenCode server 配置；本次实验使用 `http://127.0.0.1:4097`，server 从 `/home/zihang/opencode_test/SEAM` 配置启动。
- 不应把 Deepwave、GLM-OCR、vLLM、SGLang 的个案修复硬编码进框架；框架层改动必须保持泛化。
- workflow 中 `cleanup: false` 或测试中保留输出时，容器和 output project 可能不会自动删除，复测前需人工确认和清理。
- 文中 MACA/MUSA 复现实验结果不等价于 PPU/NPU 可直接复现；PPU/NPU 需要对应 image、device、env 和 workflow 适配。
